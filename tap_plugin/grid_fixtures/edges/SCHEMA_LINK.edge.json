{
  "slug": "SCHEMA_LINK__grid_fixtures",
  "name": "Schema Link",
  "description": "Constrained edge carrying a property_schema with an enum + boolean (tests edge-property validation).",
  "sources": [
    "grid_fixtures__constrained_source"
  ],
  "targets": [
    "grid_fixtures__dual_endpoint"
  ],
  "property_schema": {
    "type": "object",
    "properties": {
      "proficiency": {
        "type": "string",
        "enum": [
          "novice",
          "apprentice",
          "master"
        ]
      },
      "primary": {
        "type": "boolean"
      }
    }
  }
}
